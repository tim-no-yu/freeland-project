node_modules

# Vercel CLI link (only if you run `vercel` from repo root)
.vercel/

# Local env (do not commit secrets / per-dev config)
.env.local
.env.*.local

# Stray Next.js artifacts created if `next` is run from the repo root
/.next/
/next-env.d.ts
/src/
